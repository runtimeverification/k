function main():void {
  var e:int = 100;
  try {
    var x:int = 5;
    if (x >= 0) then throw x+2;
    print(20);      // should not be printed
  } catch(e) {
    print(e," ");   // should print 7
  }
  print(e," ");     // should print 100
  print(10,"\n");
}

// 7 100 10
