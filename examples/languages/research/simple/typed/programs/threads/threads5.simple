// Well-synchronized program which has only one behavior.

function main():void {
  var x:int = 1;
  spawn {
    acquire 1;
    x=x+1;
    release 1;
    rendezvous 1;
  }
  acquire 1;
  x=x+1;
  release 1;
  rendezvous 1;
  print(x,"\n");
}

// 3
