((fix f (lambda x . (lambda y . fif (x f== 0) fthen
                                  y
                                felse
				  (let nx = x f- 1 in
				   (let ny = y f+ x in
                                    (L2FExp mylabel : ((f nx) ny)))))))
                                     #symInt(ni)) #symInt(se)
