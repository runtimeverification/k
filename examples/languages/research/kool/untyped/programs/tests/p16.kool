class i {
  var f;
  method i(v) {
    f = v;
    return 0;
  }
  method get() { return f; }
}
class g {
  var o;
  method g(o) { // cannot use obj as id as it conflicts with obj from definition
    this.o = o;
    return 0;
  }
  method do1() { // cannot use do as id as it conflicts with do from definition
    var f;
    f = 9;
    return (o.get());
  }
}
class main {
  method main() {
    var t;
    var y;
    t = new i(1);
    y = new g(t);
    print(y.do1());
    return 0;
  }
}

// 1


