function main() {
  var j=0;
  for(var i=0; i<=5; ++i) {
    print(i, "\n");  // should print a different i at each iteration
    if(++j==10) {break;}
    continue;
    print("Wrong!\n");
  }
  print(j, "\n"); // if j is 10 here, then continue does NOT work well
}
