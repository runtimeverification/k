program1:

var i;
i := symInt(i0);
while (i <= n) do (
  s := s + i * i;
  i := i + 1
// ; z := 0
)

program2:
var i, k;
i := symInt(i0);
k := symInt(i0) * symInt(i0);
while (i <= n) do (
  s := s + k;
  k := k + i + i + 1;
  i := i + 1
// ; z := 1
)

observers: n, s;


