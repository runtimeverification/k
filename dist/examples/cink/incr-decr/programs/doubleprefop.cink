/*
  This example tests the semantics of the prefixed increments, that 
  returns a l-value.
  The output should be 2 2 .
*/
#include <iostream>
using namespace std;

int main(){
//  int z = 0, x = 0;
int z, x;
cin >> x;
  z = (++x);
  cout << z << x << endl;
  return 0;
}
