int f(int x, int &y) {
  x = 1;
  y = 2;
  return x;
}

void g(int &x, int &y) {
  x = 3;
  y = 4;
}

int main () {
  int x;
  int y;
  cout << "x, y: ";
  cin >> x >> y;
//  f(x, y);
  cout << f(x, y) << x << y << "\n";
  g(y, x);
  cout << x << y << "\n";
}
