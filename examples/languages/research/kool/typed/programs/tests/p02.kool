class c {
  var x:int;
  method c() {  // constructor method, so no need to type it
    x=5;
  }
  method g():int {
    return x;
  }
}

class main {
  method main() {
    print((new c()).g());
  }
}
