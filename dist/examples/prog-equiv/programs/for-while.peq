<k>
  L : for i from symInt(a1) to symInt(a2) do symStmt(s)
</k>
<env>
  i -> symInt(i0)
</env>
~
<k>
 L :	while (i <= symInt(a2)) do (
	  symStmt(s);
	  i := i + 1
	)
</k>
<env>
  i -> symInt(a1)
</env>

if

i isNotModifiedBy symStmt(s)


using observers: ;


