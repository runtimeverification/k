program1:

	var ;	
	for i from symInt(a1) to symInt(a2) do symStmt(s)

program2:

	var ;
	i := symInt(a1);
	while (i <= symInt(a2)) do (
	  symStmt(s);
	  i := i + 1
	)

condition:

	i isNotModifiedBy symStmt(s)


observers: i;

