package org.kframework.parser

import org.kframework.attributes.Att
import org.kframework.definition._

class EKoreToKore {

  def apply(ekoreAndKore: Module) = {
    Module(
      "EKORE-TO-KORE",
      Set(),
      Set(

      ),
      Att()
    )
  }
}
