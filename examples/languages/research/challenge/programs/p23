{
   var genpow;
   genpow = lambda n . {
              if n <= 0
              then return quote 1;
              else return quote (x * unquote (genpow (n+-1)));
            };
   var cube;
   cube = lambda x . { return eval genpow(3);};
   print cube(5);
   print cube(10);
 }

