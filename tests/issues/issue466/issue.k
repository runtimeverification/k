module LAMBDA

  syntax Val ::= "closure(" K ")" [latex(a)]
  syntax Val ::= "closure(" K ")" [latex(a), klabel('closure)]
  syntax Val ::= closure(K)
  syntax Val ::= closure(K) [latex(a)]

endmodule
