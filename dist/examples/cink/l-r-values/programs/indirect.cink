/*
  This example tests the semantics of the indirect operator.
  Note that y is initialized with the address of x, so any 
  modification of *y affects in fact x.
  The output should be 
  10
  10
*/

int main() {
  int x, *y = &x;
  *y = 10;
  cout << x << "\n";
  int z = *y;
  cout << z << "\n";
}
