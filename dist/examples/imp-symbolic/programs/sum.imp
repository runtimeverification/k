// This program calculates in sum
// the sum of numbers from 1 to n.

<k>
int i,sum;
sum = 0;
i   = 1;
while (i <= n) {
  sum = sum + i;
  i = i + 1;
}
</k>
<env> n -> symInt(n) </env>
<condition> 0 <= symInt(n) && symInt(n) <= 3 </condition>

// sum should be 5050 when n is 100
