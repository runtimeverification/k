K Tutorial --- IMP++ --- Tagging; Superheat/Supercool Kompilation Options
by Grigore Rosu

- add the rule for increment
- krun div.imp
- krun div.imp --search; only one behavior
- add division, lookup, increment tags
- kompile imp.k --superheat="division" --supercool="lookup increment"
- krun div.imp --search; see the five behaviors
- the use of kompile options can generate more comprehensive, but slower models
- more on tags: several rules can have the same tag
- more on tags: by default, krun option names used as tags are automatically
  added to that option when executing krun
