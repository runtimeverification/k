#include <stdio.h>
int f(int n) {
       int x;
       x = n;
       return x;
    }
    int main() {
       int i;
       int n;
       scanf("%d", &n);
       if (n > 0) {
          f(i);
       }
    }
