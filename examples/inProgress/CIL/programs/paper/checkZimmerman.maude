load a.out
--- load ../../../model-checker
mod MODEL-CHECK-TEST is
	including C-program-zimmermann .
	op modelBag : -> Bag .
	eq modelBag = eval('program-zimmermann(.List{K}), String "a.out"(.List{K}), "") .
	
endm
search modelBag =>! B:Bag . --- mutual exclusion and no deadlock

q
