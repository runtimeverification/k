var n, m;

function divide2(dummy1, r, dummy2) {
	var q = 0;
	var d;
	
	while not r <= 1 do {
		r = r - 2;
		q = q + 1;
	}
	d;
	return q;
}


function collatz() {
	var s=0;
	write(n);
	while n > 1 do {
	s = s+1;
	if n == (n/2)*2
		then n = divide2(3+n, n, true or false);
		else n = 3*n+1;
	}
	return s;
}

function main() {
	m = 10;
	for i = 1 to m do {
		n = i;
		write(collatz());
	}
}
