// This is a poorly synchronized program, which has lots of behaviors.

function main():void {
  var x:int = 5;
  spawn x=x+2;
  spawn x=x*2;
  spawn x=11;
  spawn print(x,"\n");
}

// 5, but the search option of krun finds 42 solutions in total!
