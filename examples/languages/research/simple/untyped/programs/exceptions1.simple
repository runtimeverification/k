function main() {
	var e = 100;
	try {
		var x = 5;
		if (x >= 0) then throw x+2;
		write(20);  // should not be written
	} catch(e) {
		write(e);   // should write 7
	}
	write(e);     // should write 100
	write(10);
}
// 7 100 10
