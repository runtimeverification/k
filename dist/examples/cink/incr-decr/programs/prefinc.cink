/*
  This example exhibits different uses of the prefixed increment operator.
*/
#include <iostream>
using namespace std;

int main() {
  int x = 1,y, z;
  y = ++x + 3;
  cout << "x = " << x << "; y = " << y << endl;
  return 0;
} 
